export const getServerUrl = (): string => {
  // In development, this might differ. For now, assume relative path or env var.
  // If you are running locally with a separate backend port:
  // return 'http://localhost:3000'; 
  return '';
};